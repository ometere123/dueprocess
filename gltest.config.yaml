networks:
  default: localnet

  localnet:
    url: http://127.0.0.1:4000/api
    leader_only: false

  studio_devnet:
    # gltest v0.30 RC built-in name for the Consensus v0.6 Studio preview.
    # This preset resolves to chain 61997 / the Studio development deployment.
    leader_only: false

paths:
  contracts: contracts
  artifacts: artifacts

environment: .env
