# Deployment evidence

Status: **SOURCE READY — LIVE DEPLOYMENT EVIDENCE NOT YET RECORDED**

This file is intentionally not populated with invented addresses or transaction hashes.

## Target deployment

Deploy both contracts to the current GenLayer Studio/StudioNet environment:

1. `contracts/dueprocess.py`
2. `contracts/protected_executor.py` with the finalized DueProcess address as constructor input.

## Record after deployment

### DueProcess

- Network:
- Contract address:
- Deployment transaction:
- Consensus result:
- Finalized at:

### ProtectedExecutor

- Network:
- Contract address:
- Deployment transaction:
- Consensus result:
- Finalized at:

## Lifecycle evidence

Record exact finalized transaction links for:

- charter creation/seal;
- valid process start;
- each valid step attempt;
- valid process finalization;
- successful consumer execution;
- invalid process start;
- premature authorized step attempt;
- invalidation;
- failed consumer execution against invalid process.

See `docs/REVIEWER_DEMO.md` for the exact review path.
